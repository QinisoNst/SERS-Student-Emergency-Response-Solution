
{
  "name": "firebase-frameworks-nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm run build",
    "start": "cd app && npm run start",
    "lint": "cd app && npm run lint",
    "deploy": "npm run build && firebase deploy",
    "genkit:dev": "cd app && npm run genkit:dev",
    "genkit:watch": "cd app && npm run genkit:watch",
    "typecheck": "cd app && npm run typecheck"
  },
  "dependencies": {},
  "devDependencies": {
    "firebase-tools": "^13.15.2"
  }
}
